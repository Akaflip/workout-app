import React from 'react'
import Header from '../../common/Header/Header'
import Layout from '../../common/Layout'

export const Home = () => {
  return (
    <Layout>
      <Header />
    </Layout>
  )
};

export default Home;
